// import handleShowOverlay from './handle-show-overlay';

export default async function handleClicks(event) {
  console.log('clicked', event.target);
  // if (event.target.matches('[data-action="show-overlay"]')) {
  //   return handleShowOverlay(event);
  // }
}